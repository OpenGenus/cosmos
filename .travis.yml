language: c
compiler:
  - clang
  - gcc

before_script:
  chmod +x ./configure

os: linux
  addons:
    apt:
      sources:
        - ubuntu-toolchain-r-test
        - llvm-toolchain-trusty-5.0
      packages:
        - g++-7
        - clang-5.0
