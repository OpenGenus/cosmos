(defun square()
(princ "Enter Number: ")
(setq n (read))
(setq i 0)
(loop 
  (if (>= (* i i) n)
    (return 0)
  )
  (incf i)
)
(if (equal (* i i) n) 
  (princ "Number is a square")
(princ "Number is not a square")
)
)
(square)
