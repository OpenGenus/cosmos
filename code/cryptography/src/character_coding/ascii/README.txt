# cosmos
> Your personal library of every algorithm and data structure code that you will ever encounter

Encoders are vital part of cryptography so we gather some of the most common character encoding algorithms.

Suported encoders:

>ascii
>utf8
>utf16
>utf32
>huffman

The encoders are taking as parameter a string and return an array with the encoded characters.